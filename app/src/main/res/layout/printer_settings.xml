<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/login_REL_content"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#f5f5f5"
    android:paddingBottom="12dp"
    android:paddingLeft="12dp"
    android:paddingRight="12dp" >

    <include
        android:id="@+id/top_bar"
        layout="@layout/top_bar_layout" />

    <RelativeLayout
        android:id="@+id/content"
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/top_bar"
        android:layout_marginTop="20dp"
        android:background="#f5f5f5" >

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <CheckBox
                android:id="@+id/settings_CHK_signature_required"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:checked="false"
                android:text="signature required"
                android:textSize="22sp" />

            <CheckBox
                android:id="@+id/settings_CHK_printer_support"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:checked="false"
                android:text="printer support"
                android:textSize="22sp" />

            <CheckBox
                android:id="@+id/settings_CHK_print_signature"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:checked="false"
                android:text="print signature"
                android:textSize="22sp" />

        </LinearLayout>
    </RelativeLayout>

</RelativeLayout>